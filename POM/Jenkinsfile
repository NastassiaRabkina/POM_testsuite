pipeline {
    agent { docker { image 'python:3.10.7-alpine' } }
    stages {
        stage('build') {
            steps {
                sh 'pip3 install -r requirements.txt'
                sh 'pytest --environment=test test_login_form.py'
            }
        }
    }
}
